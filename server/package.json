{
  "name": "server",
  "version": "0.0.1",
  "description": "server to connect clients",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "thymajesty",
  "license": "ISC",
  "dependencies": {
    "peer": "^0.2.10"
  }
}
