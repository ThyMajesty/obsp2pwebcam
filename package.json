{
  "name": "server",
  "version": "0.0.1",
  "description": "server to connect clients",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "thymajesty",
  "license": "ISC",
  "dependencies": {
    "peer": "^0.2.10"
  },
  "engines": {
    "node": "10.x"
  }
}
